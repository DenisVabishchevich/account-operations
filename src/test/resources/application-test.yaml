spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test;mode=PostgreSQL;DB_CLOSE_DELAY=-1
    password: sa
    username: sa
  jpa:
    hibernate:
      show-sql: true
    properties:
      hibernate:
        format_sql: true
